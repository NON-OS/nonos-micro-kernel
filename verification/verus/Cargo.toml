# NONOS Operating System
# Copyright (C) 2026 NONOS Contributors
#
# Verus proof crate. Checked with the Verus verifier, not run. See
# verification/verus/README.md for the exact toolchain and command.
[package]
name = "nonos-verus-proofs"
version = "0.2.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

# vstd ships with the Verus toolchain; `cargo verus` supplies it on the verifier
# sysroot, so it is not pinned as a normal registry dependency here.
[dependencies]
