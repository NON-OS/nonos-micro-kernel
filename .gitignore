# Build outputs
target/
*.iso
*.img
*.bin
!firmware/*.fd
!nonos-bootloader/firmware/**/*.bin

# IDE and editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Node
node_modules/

# Secrets and keys { NEVER COMMITTED }
/keys/
.keys/*
!.keys/.gitignore
!src/**/keys/
*.key
*.pem
*_key.bin
*_key.json
signing_key*.bin
signing_key*.json
dev_signing_key.bin
*.secret
# Belt and suspenders: refuse seed files anywhere in the tree.
*.seed

# NØNOS baked trust artifacts ship under nonos-data/trust/ so CI
# can reverify the policy + per-capsule cert + manifest without
# secrets. Seeds remain ignored above.
!nonos-data/trust/
!nonos-data/trust/**
!nonos-data/trust/**/*.bin
!nonos-data/trust/**/*.pub

# ZK artifacts (generated, large)
zk-artifacts/generated/
zk-artifacts/*.bin
zk-artifacts/*.json
!zk-artifacts/README.md
generated_keys/

# Logs and temp files
*.log
*.tmp
*.temp

# Build cache
.cargo/
.rustup/

# Test outputs
test-results/
coverage/

# Local config overrides
.env
.env.local
*.local.toml

# AI assistant instruction files (local overrides, not for version control)
.claude/
CLAUDE.md
CONTEXT.md
.cursorrules
.cursorignore
.aider*
.continue/
.superpowers/

