[package]
name = "nonos_nox_receipt"
version = "0.1.0"
edition = "2021"
description = "Verify a NOX ERC20 payment from its transaction receipt, no_std"
license = "AGPL-3.0"

[lib]
crate-type = ["rlib"]

[dependencies]
