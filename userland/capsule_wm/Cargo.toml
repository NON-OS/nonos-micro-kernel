# NONOS userland: window manager

[package]
name = "nonos_wm"
version = "0.3.0"
edition = "2021"
authors = ["NONOS Contributors"]
license = "AGPL-3.0"
description = "NONOS window manager capsule skeleton"

[[bin]]
name = "wm"
path = "src/main.rs"

[dependencies]
nonos_libc = { package = "nonos_userland_libc", path = "../libc" }
