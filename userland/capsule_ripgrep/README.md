# capsule_ripgrep

Ripgrep tool capsule contract. Manifest mask: `CAPSULE_REQUIRED_CAPS = 0x19`.
The capsule runs as a signed tool endpoint and must use Mk IPC/memory/syscall
interfaces only; it has no hardware or persistence authority.
