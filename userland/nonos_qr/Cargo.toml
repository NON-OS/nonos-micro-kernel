[package]
name = "nonos_qr"
version = "0.3.0"
edition = "2021"
description = "QR code encoder (byte mode, ISO/IEC 18004) for the NONOS wallet, no_std"
license = "AGPL-3.0"

[lib]
crate-type = ["rlib"]

[features]
# Host proof-of-concept build: pulls in std for the PNG render + decode test.
std = []
