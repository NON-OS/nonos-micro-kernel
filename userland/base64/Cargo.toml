[package]
name = "nonos_base64"
version = "0.3.0"
edition = "2021"
authors = ["NONOS Contributors"]
license = "AGPL-3.0"
description = "Shared base64 encode/decode for NONOS userland"

[lib]
name = "nonos_base64"
path = "src/lib.rs"

[dependencies]
