[package]
name = "nonos_git"
version = "0.2.0"
edition = "2021"
description = "Git object model, hashing and object store for the NONOS terminal, no_std"
license = "AGPL-3.0"

[lib]
crate-type = ["rlib"]

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
